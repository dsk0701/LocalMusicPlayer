import SwiftUI

struct PlayerControlBar: View {
    var body: some View {
        HStack {
            // TODO:
            // 巻き戻し
            // 再生・一時停止
            // 早送り
        }
    }
}

#Preview {
    PlayerControlBar()
        .background(Color.black)
}
